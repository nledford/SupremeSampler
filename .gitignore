# Generated by xcodegen from project.yml -- do not hand-edit or commit.
# Run `just xcode` (or `xcodegen generate`) to (re)create it.
SupremeSampler.xcodeproj/

# Xcode
DerivedData/
*.xcuserstate

# Swift Package Manager (if any SPM dependencies are added later)
.build/
.swiftpm/

# macOS
.DS_Store

# Packaging output (just app / just dmg)
dist/

# Impeccable design-critique snapshots: local notes, and they quote the
# real catalog's figures (counts, paths).
.impeccable/
